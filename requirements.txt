# Python dependencies for thorn-cli.py and the builder (bab package)
# Install: pip install -r requirements.txt

requests>=2.28
urllib3>=1.26
pycryptodome>=3.18

# Post-exploitation (cli/post.py) — impacket wrappers
# Optional: only needed for secretsdump, kerberoast, psexec, wmiexec, etc.
impacket>=0.11
